// ============================================
// 預設值(可選)— 主要設定請從 APP 內的「⚙️ 設定」輸入
// ============================================
//
// 從 v2 開始,API key 統一存在瀏覽器的 localStorage,
// 不再寫在這個檔案裡。這樣即使你把整個資料夾上傳到 GitHub,
// 也不會洩漏 API key。
//
// 第一次打開 APP 時,會自動跳出設定視窗請你輸入 key。
//
// 下面這幾個值是「預設值」,只在 localStorage 還沒有設定時使用:

const CONFIG = {
  // 不要在這裡填 API key — 從 APP 的「⚙️ 設定」輸入,
  // 設定會存在你裝置的瀏覽器,不會跟著檔案被分享出去
  API_KEY: "",

  // 預設模型(可在 APP 內切換)
  MODEL: "claude-sonnet-4-6",

  // 母語
  NATIVE_LANGUAGE: "繁體中文",
};
